DROP TABLE WORLDBANK_JSON_HDFS;
CREATE TABLE WORLDBANK_JSON_HDFS
  (doc CLOB)
  ORGANIZATION EXTERNAL
  (TYPE ORACLE_HDFS
   DEFAULT DIRECTORY DEFAULT_DIR
   LOCATION ('/user/oracle/jsonfiles/world_bank.json')
  )
  REJECT LIMIT UNLIMITED;

DROP TABLE STOCK_JSON_HDFS;
CREATE TABLE STOCK_JSON_HDFS
  (doc CLOB)
  ORGANIZATION EXTERNAL
  (TYPE ORACLE_HDFS
   DEFAULT DIRECTORY DEFAULT_DIR
   LOCATION ('/user/oracle/jsonfiles/stocks.json')
  )
  REJECT LIMIT UNLIMITED;
  
DROP TABLE COMPANY_JSON_HDFS;
CREATE TABLE COMPANY_JSON_HDFS
  (doc CLOB)
  ORGANIZATION EXTERNAL
  (TYPE ORACLE_HDFS
   DEFAULT DIRECTORY DEFAULT_DIR
   LOCATION ('/user/oracle/jsonfiles/companies.json')
  )
  REJECT LIMIT UNLIMITED;
  
